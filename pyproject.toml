[project]
name = "mms-client"
version = "0.1.0"
description = "IEC 61850 IED client (with a pluggable MMS protocol module) for verifying and troubleshooting IEDs in a test rack"
readme = "README.md"
requires-python = ">=3.12,<3.13"
license = { text = "GPL-3.0-or-later" }
dependencies = [
    # The MMS protocol module's stack. Pinned exactly: its adapter binds to symbols of the
    # libiec61850 build shipped inside this wheel (see docs/spikes.md).
    "pyiec61850-ng==1.6.1.10",
    "prompt_toolkit>=3.0.40",
    "rich>=13.7",
    "PyYAML>=6.0.1",
]

[project.scripts]
mms-client = "ied_client.cli.main:main"

# Protocol modules the client can drive (IED-CLIENT-SPEC.md §4.1). A module registers itself here
# with the ProtocolModule object it exports; the client never imports a module by name.
[project.entry-points."ied_client.protocols"]
mms = "mms_protocol:protocol"

[dependency-groups]
dev = [
    "pytest>=8.2",
    "pytest-timeout>=2.3",
    "ruff>=0.6",
]

[build-system]
requires = ["hatchling>=1.24"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ied_client", "src/mms_protocol"]

[tool.pytest.ini_options]
testpaths = ["tests"]
timeout = 120
markers = [
    "integration: needs the simulated IED (spawns a subprocess on localhost)",
    "soak: long-running memory soak test (RSK-5); run with -m soak",
]
addopts = "-m 'not soak'"

[tool.ruff]
line-length = 110
target-version = "py312"
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "B", "UP", "SIM"]
ignore = ["E501", "SIM105", "SIM108"]
